
/**
 * Dummy Payload config shim to resolve build errors.
 * The project is migrating away from Payload, and this file satisfies
 * the imports while we transition to a stubbed backend.
 */
export default {
  // Minimal config object
} as any;
