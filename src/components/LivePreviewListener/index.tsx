/* eslint-disable */
"use client";

// LivePreviewListener — payload live preview removed in KalpGo architecture.
// This component is a no-op stub that was previously used for Payload CMS live preview.
// It can be safely removed from layouts; kept here only to prevent import errors.
export const LivePreviewListener = () => {
  return null;
};
